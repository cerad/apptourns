security:
    hide_user_not_found: false

    encoders:
        Symfony\Component\Security\Core\User\User: plaintext
        Cerad\Bundle\AccountBundle\Entity\User: 
            id: cerad_account.user_encoder
        
    role_hierarchy:
        ROLE_ASSIGNOR:    [ROLE_USER, ROLE_ASSIGNOR]
        ROLE_ADMIN:       [ROLE_USER, ROLE_ASSIGNOR]
        ROLE_SUPER_ADMIN: [ROLE_USER, ROLE_ASSIGNOR, ROLE_ADMIN, ROLE_ALLOWED_TO_SWITCH]

    providers:
        in_memory_user_provider:
            memory:
                users:
                    user:     { password: user894,  roles: [ 'ROLE_USER' ]  }
                    admin:    { password: admin894, roles: [ 'ROLE_ADMIN' ] }
                    su:       { password: su894,    roles: [ 'ROLE_SUPER_ADMIN'] }
                    assignor: { password: assign,   roles: [ 'ROLE_ASSIGNOR'] }
                    
        cerad_account_user_provider:
            id: cerad_account.user_provider
            
        chained_user_provider:
            chain:
                providers: [ in_memory_user_provider, cerad_account_user_provider ]
                
# ================================================================================
    firewalls:
        dev:
            pattern:  ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            pattern:   ^/         
            anonymous: ~
            
            form_login:
                provider:            cerad_account_user_provider
                login_path:          cerad_tourn_account_login
                check_path:          cerad_tourn_account_login_check
                default_target_path: cerad_tourn_home

                username_parameter:  cerad_account_login[username]
                password_parameter:  cerad_account_login[password]
                csrf_parameter:      cerad_account_login[_token]
                csrf_provider:       form.csrf_provider
                intention:           authenticate
                
            logout:
                path:   cerad_tourn_account_logout
                target: cerad_tourn_welcome
                                
            remember_me:
                key:      "%secret%"
                lifetime: 31536000  # 365 days in seconds
                path:     cerad_tourns_welcome
                domain:   ~
                name:     zayso_remember_me
                remember_me_parameter: cerad_account_login[_remember_me]

    access_control:
        - { path: '^/[A-Za-z0-9_-]+/admin',roles: ROLE_ASSIGNOR }


